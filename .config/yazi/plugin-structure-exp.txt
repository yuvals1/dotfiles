 -- Return the plugin table
  return {
      setup = setup,
      fetch = fetch,
      entry = entry
  }

  - fetch = "Get info about these files that are about to be displayed"
  - entry = "User pressed a key, do something"
  - fetch runs automatically, entry runs on keypress

