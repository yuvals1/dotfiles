# List of base packages to install via apt
curl
wget
git
build-essential
zsh
ca-certificates
gnupg
lsb-release
stow
libsqlite3-dev
sqlite3
htop

# Python (needed for Mason to install black, isort, mypy, etc.)
python3-pip
python3-venv

# Lua (needed for Mason to install luacheck)
luarocks

# Add other packages as needed...

